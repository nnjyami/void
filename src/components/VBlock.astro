---
import type { HTMLAttributes } from "astro/types";

const { column, ...props } = Astro.props;
const columnClassName = `column column-${column}`;
---

<section class={columnClassName}>
  <slot />
</section>

<style>
  section {
    width: 100%;
    writing-mode: vertical-rl;

    font-size: 16px;
    font-family: fot-tsukuaoldmin-pr6n, sans-serif;
    font-weight: 300;
    font-style: normal;
    font-feature-settings: "vpal";
    text-align: justify;
    text-justify: inter-ideograph;
    letter-spacing: 0.08em;
    line-height: 2;
    vertical-align: top;

    column-fill: balance;
    column-count: auto;
    column-width: 14em;
    column-gap: 0.1em;
    height: auto;

    margin: 0;
    padding: 0;
  }
  p {
    padding: 0;
    margin: 0 0 1em;
  }
  .column-1 {
    column-count: 1;
    height: 18em;
  }
  .column-2 {
    column-count: 2;
    height: 38em;
  }
  @media (max-width: 720px) {
    section {
      font-size: 16px;
      column-gap: 0.2em;
    }
  }
</style>
